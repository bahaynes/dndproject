#!/usr/bin/env bash
# Pre-push hook: run tests before pushing to remote.
# Installed via: git config core.hooksPath .githooks
# (setup.sh handles this automatically)
set -e

exec "$(git rev-parse --show-toplevel)/run_tests.sh"
